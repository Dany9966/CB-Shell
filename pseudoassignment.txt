#v0.1 : first check unrecursively if files in folder have occurances

save file list
for every item in filelist check if regular file or directory
    if directory continue
    else if regular then check if has occurance and print file name

##############################

#v0.2

trying to make it recursive

##############################

#other functions
both_funcs(dirorregfile)
	verify which is:
	       if dir then ls and recurse with each inside
	       else if reg and tar then extract in auxdir ; cd auxdir ; and do what dir does
	       	    else check for occurences ; print 

n_of_c_func(dirorregfile, option):
    


case # in
     1)
	if isreg($1)
	   then if isTar($1)
	   	then

	if isdir($1)

	;;

     2)
	two_arg_function()

     3)
	reexeceute with 1 arg


